#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"

if [ -z "${JAVA_HOME:-}" ]; then
  CANDIDATE="$HOME/.local/share/mise/installs/java/17.0.2"
  if [ -d "$CANDIDATE" ]; then
    export JAVA_HOME="$CANDIDATE"
  fi
fi

exec gradle -p "$DIR" "$@"
