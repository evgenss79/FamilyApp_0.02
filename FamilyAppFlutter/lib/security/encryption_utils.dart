/// Provides simple encryption and decryption helpers.  In this
/// simplified example the functions perform identity transformations
/// and are present solely to satisfy imports in other parts of the
/// application.
class EncryptionUtils {
  /// Returns the input string unchanged.
  static String encrypt(String input) => input;

  /// Returns the input string unchanged.
  static String decrypt(String input) => input;
}