/// Handles signalling and connectivity for cloud-based calls.  This
/// simplified implementation provides only a stub method to satisfy
/// dependencies.
class CloudCallService {
  Future<void> connectCall(List<String> participantIds) async {
    // Real implementation would connect a call via signalling server.
    return;
  }
}